{
  "name": "4minit",
  "version": "1.0.0",
  "description": "4minit — Mauritius Morning Brief",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "pipeline:pre":  "node pipeline.js --pre",
    "pipeline:post": "node pipeline.js --post && node scripts/build-static.js && node scripts/generate-sitemap.js && node scripts/generate-feed.js",
    "build:static":  "node scripts/build-static.js",
    "build:sitemap": "node scripts/generate-sitemap.js",
    "build:feed":    "node scripts/generate-feed.js",
    "build":         "node scripts/build-static.js && node scripts/generate-sitemap.js && node scripts/generate-feed.js"
  }
}
